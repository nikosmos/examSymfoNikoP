<!DOCTYPE html>

<title>Edit Brand</title>

{% block body %}
    <h1>Edit Brand</h1>

    {{ include('back/pages/brand/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_brand_index') }}">back to list</a>

    {{ include('back/pages/brand/_delete_form.html.twig') }}
{% endblock %}
